// Sub-visible film grain that ties the cream + navy bands into one
// warm-lit surface. Pure CSS (.grain), static, pointer-events:none.
export function GrainOverlay() {
  return <div aria-hidden className="grain" />;
}
